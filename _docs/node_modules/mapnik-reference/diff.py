#!/usr/bin/env python

try:
    # <= python 2.5
    import simplejson as json
except ImportError:
    # >= python 2.6
    import json


if __name__ == "__main__":
    a = '2.0.1'
    b = '2.1.0'
    reference_a = json.load(open('%s/reference.json' % a, 'r'))
    reference_b = json.load(open('%s/reference.json' % b, 'r'))
    for sym in reference_a['symbolizers'].items():
        sym_group = sym[0]
        if not reference_b['symbolizers'].has_key(sym_group):
            print 'nope!'
